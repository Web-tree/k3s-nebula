# Basic Example

This example demonstrates how to use the specific `k3s-nebula` modules to provision a simple cluster.

## Usage

1. Copy `terraform.tfvars.example` to `terraform.tfvars`.
2. Fill in your own values (do not use placeholders).
3. Run Terraform.

```bash
terraform init
terraform apply
```
